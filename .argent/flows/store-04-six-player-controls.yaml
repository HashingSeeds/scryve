# Five-player board with the radial game menu open, showing the per-game controls.
# Continues from store-02.
steps:
  - launch: com.sowinghope.count.preview
  - await: { idle: true }
  - echo: Resume the active five-player game left by store-02.
  - await: { visible: { id: player-grid } }
  - tap: { id: game-menu-button }
  - await: { idle: true }
